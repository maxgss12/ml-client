import './nav.scss';
import SearchBox from "./search-box/search-box.component";

const Nav = () => (
  <header>
    <nav className="nav">
      <SearchBox/>
    </nav>
  </header>
)

export default Nav
